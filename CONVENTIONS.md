# مواضعات Car Hero — حقائق مُثبتة بالتجربة

## سجلّ تنفيذ UI_UX_PROMPTS.md

**منجَز: ١ – ٤٤ — الوثيقة كاملة.** الأقسام الثمانية كلّها مرّت بجولة تحسين
وتحقّق حيّ على المتصفّح مقابل خادم حقيقي.

**المتبقّي ليس شاشات بل قرارات مالك المنتج** (انظر «قرارات معلّقة» أسفل هذا
الملف)، ومعها جولة مراجعة انحدارات على المسارات الحرجة عند الحاجة.

**طريقة العمل المتّفق عليها مع المالك:**
- دفعات من ٢–٣ شاشات متقاربة، لا شاشة واحدة.
- جولة تحقّق بصري واحدة لكل دفعة (لا لكل حالة).
- التقرير = قائمة تغييرات موجزة + ما لم يُنفَّذ ولماذا. لا الأقسام الخمسة الكاملة.
- ابدأ كل دفعة بقراءة هذا الملف، ثم برومبت الشاشة من `newapp2/UI_UX_PROMPTS.md`
  (الكتلة صفر فيه هي السياق الحاكم).

**فحص سريع بعد كل تعديل** (أسرع من تشغيل الحزمة):
```
cp <file> "$TEMP/chk.jsx" && npx esbuild --outfile=/dev/null "$TEMP/chk.jsx"
```


مرجع مختصر لما اكتُشف أثناء العمل على شاشات التطبيق. كل بند هنا **مُتحقَّق منه
في البناء الحيّ**، لا مفترض.

## react-native-web (0.21) — مصائد صامتة

- **`accessibilityElementsHidden` و`importantForAccessibility` مُتجاهَلتان تماماً.**
  لا تُترجَمان إلى `aria-hidden`، فيبقى العنصر معلَناً لقارئ الشاشة.
  **استخدم `aria-hidden`** — مدعومة أصلاً في RN 0.81 على كل المنصّات.
- **`props.pointerEvents` مهجورة** في RN 0.81 → ضعها في النمط: `style={{ pointerEvents }}`.
- **`accessibilityState` لا يُترجَم إلى ARIA** — يعالجه `ariaFromState` داخل
  `PressableScale`، فمرّر الحالة عبره لا عبر `Pressable` مباشرة.
- **`direction` ليست خاصية نمط صالحة** في RN وتطبع تحذيراً.
- الصور الزخرفية: `alt=""` هو الآلية الصحيحة على الويب.

## RTL

- التطبيق **لا يفرض** `I18nManager.forceRTL`؛ الاتجاه يُبنى يدوياً
  بـ `flexDirection: "row-reverse"` و`textAlign: "right"`.
- **في الشرائح الأفقية: السحب يميناً = التالي.** الشريحة التالية تقع *يسار*
  الحالية، أي إزاحتها `-pageWidth`. سهم «التالي» يشير يساراً، و«السابق» يميناً.
- `ScrollView` أفقي **لا يُسحب بالفأرة على الويب** — أي شريط شرائح يحتاج
  أزرار تنقّل مرئية، أو `PanResponder` (يعمل بالفأرة على الويب).

## عقود الخادم — لا تخترع قواعد أشدّ

- **كلمة المرور:** `RegisterDto` يطلب ٨ رموز + حرف كبير + رقم **فقط**.
  لا رمز خاص ولا حرف صغير. `PASSWORD_RULES` تفصل `required` عن التوصيات.
- **الهاتف:** `+963` + ٩ أرقام تبدأ بـ ٩.
- **`isTermsAccepted`** إلزامي ويجب أن يساوي `true` — ويُرسل من موافقة
  المستخدم الفعلية، لا كقيمة ثابتة.
- **NestJS يرفض عدّة حقول في مصفوفة `message` واحدة**، و`api.js` يأخذ أوّلها
  فقط. المصفوفة الكاملة في `error.raw` → استخدم `mapServerFieldErrors`.
- `GET /providers/nearby` يُرجع المزوّدين **المتصلين (`online`) فقط** — الفراغ
  ليس بالضرورة عطلاً.
- `GET /reviews/provider/:id` **يتطلّب توكن** (`{ auth: true }`).
- **`provider.totalReviews` غير متزامن ويكذب** — استخدم `total` العائد مع القائمة.
- **`GET /orders` يُرجع `facets.statusCounts`** مع كل استعلام — مصدر عدّادات
  التبويبات. لكنها تُحسب **بعد** التصفية، فاطلبها بنداء بلا `statuses`.
- **الحجز المسبق مقيَّد بـ`workingHours`:** الخادم يرفض أي موعد خارجها، ومزوّد
  بلا ساعات منشورة **يُرفض دائماً** (١٩ مزوّداً فقط من ١٨٧٣ ينشرونها في قاعدة
  التطوير). ولا توجد نقطة نهاية تُرجع الفتحات — التحقّق لحظة الإنشاء فقط،
  فبُنيت نفس القواعد في `services/scheduling.js` وتُعالَج ٤٠٩ كمسار بديل.
- **الوقت بتوقيت `Asia/Damascus` صراحةً:** الخادم يقرأ ساعات العمل بتوقيته
  المحلّي. كل تحويل يمرّ عبر `zonedToDate`/`zonedParts` في `scheduling.js`.
- **المركبات:** الحد ١٠، وأوّل مركبة تصير افتراضية تلقائياً، ولا يسمح الخادم
  بحذف المركبة الوحيدة إن كانت افتراضية. الحقول المطلوبة:
  `brand, model, year, color, plateNumber` (و`forbidNonWhitelisted` يرفض
  `make`/`plate` — مرّ عبر `normalizeVehicleBody`). **ولا يفحص الخادم ارتباط
  المركبة بطلب جارٍ عند الحذف** — التحذير مبنيّ في العميل من `/orders`.
- **علّة `Types.ObjectId` المتكرّرة:** المخطّطات تعلن الحقل
  `@Prop({ type: Types.ObjectId })` وهي لا تُفعّل الصبّ التلقائي في هذه النسخة،
  فيجب الصبّ صراحةً على الطرفين. أُصلح ثلاث مرّات حتى الآن:
  `status-history` (قراءة)، و`maintenance-record` (كتابة). إن ظهرت قائمة فارغة
  رغم وجود الصفوف في القاعدة — ابدأ التحقيق من هنا.
- **المحفظة — أربع حقائق تُبطل ما تبدو عليه الشاشة:**
  ١) `currency` الافتراضية `SAR` بينما كل مبالغ التطبيق بالليرة — **اعرض
  `CURRENCY` من `walletApi` دائماً، ولا تنسخ رمز الخادم**.
  ٢) `/wallet/deposit` **معطّل صراحةً** (يرمي دائماً)؛ الشحن عبر
  `/payments/initialize` فقط، وحدّه الأدنى `@Min(100)`.
  ٣) نجاح الدفع يصل الخادمَ عبر webhook — **التطبيق لا يعرف لحظة النجاح**،
  فالحسم يكون بمقارنة الرصيد قبل/بعد.
  ٤) `GET /wallet/transactions` يعيد `{data, total}` لكن `unwrapPayload` يفكّ
  الغلاف فيضيع `total` — استنتج نهاية القائمة من طول الدفعة.
- **نقاط الوفاء:** قيمة النقطة **٠٫٠٥** (`RedeemLoyaltyPointsUseCase`)، أي
  ١٠٠ نقطة = ٥ ل.س. و**الاستبدال بلا `orderId` لا يزيد الرصيد إطلاقاً**
  (الحركة تُسجَّل بـ `balanceBefore === balanceAfter`) — القيمة الوحيدة هي
  خصمها على طلب غير مدفوع. ولا يوجد في الخادم أي مسار **يكسب** نقاطاً.
- **الاشتراكات:** `GET /subscriptions/status` يعيد
  `{ isActive, subscriptionId, planId, expiresAt, daysLeft }` **فقط** — لا
  `status` ولا `autoRenew` ولا تفاصيل الخطة. الاسم والمزايا من
  `/subscriptions/plans`، وبقيّة الوثيقة (التجديد، المبلغ المدفوع، تاريخ
  البدء) من `/subscriptions/history` بمطابقة `subscriptionId`
  (`fetchCurrentSubscription` تجمعها).
- **الاشتراك يُدفع من رصيد المحفظة** ويرفضه الخادم بلا رصيد كافٍ، ومن لديه
  اشتراك نشط يحتاج `upgrade` لا `subscribe`. الشاشة تقرّر المسار مسبقاً
  وتعرض النقص كزرّ شحن بدل رسالة فشل بعد الضغط.
- **الإلغاء لا يسترد شيئاً:** `cancelSubscription` بلا `cancelImmediately`
  يوقف التجديد ويُبقي المدّة، ومعه ينهيها فوراً — ولا يوجد مسار استرداد في
  الخادم إطلاقاً. قُل ذلك قبل الشراء لا بعده.
- **الخطط تختلف مدّتها** (٣٠ يوماً مقابل ٣٦٥)، فالمقارنة الصحيحة بالسعر
  الشهري المكافئ (`monthlyEquivalent`) لا بالسعر المطلق.
- **`PATCH /users/me` يقبل `fullName` و`profileImage` و`preferences` فقط**،
  ومع `forbidNonWhitelisted` يردّ أي حقل آخر بـ ٤٠٠. **لا يوجد بريد إلكتروني
  في نموذج المستخدم أصلاً** (لا يُعاد من `GET /users/me`)، و**رقم الهاتف غير
  قابل للتعديل** — لا حقل له ولا نقطة نهاية. اعرضه معطّلاً مع السبب.
- **العناوين:** `note` (ملاحظة الوصول) جزء من العقد منذ البداية — استخدمه.
  والموقع يُخزَّن GeoJSON: `location.coordinates = [lng, lat]` (لا
  `coordinates` كما يُرسَل). أول عنوان يصير افتراضياً، وحذف الافتراضي يرقّي
  غيره تلقائياً، ولا حدّ أقصى للعدد.
- **إعادة استخدام الخريطة:** `navigation.navigate("InteractiveMap", { from })`
  ثم اقرأ ما اختاره المستخدم من `getCachedCoords()` (`source === 'manual'`).
  البلاطة الساكنة للمعاينة عبر `tileUrlFor` في `geocoding.js`.
- **`Switch` على الويب ٤٠×٢٠ ولا يُصدِر `aria-checked`** رغم
  `accessibilityRole="switch"`. الحلّ المعتمد: الصفّ كلّه هو المفتاح
  (`PressableScale` بدور switch وحالة checked)، والمفتاح داخله تمثيل بصري
  فقط: `aria-hidden` و`pointerEvents: "none"` في النمط.
- **طرق الدفع:** الأنواع `cash | card | wallet`، و**النقد لا يُحذف** (يرفضه
  الخادم)، و**إضافة بطاقة تحتاج `providerToken`** من بوّابة غير مدمجة —
  لا تعرض الخيار كأنه متاح. `providerToken` معلَّم `select:false` فلا يُعاد،
  ولا يوجد حقل صلاحية للبطاقة أصلاً.
- **تفضيلات الإشعارات ثلاث قنوات فقط** (`push`/`sms`/`email`) و
  `forbidNonWhitelisted` يردّ أي مفتاح آخر. الدافع يحترم `push` فعلياً.
  **الفصل بين «تحديثات الطلب» و«العروض» غير مدعوم** — يحتاج حقلين جديدين
  في المخطّط والـ DTO **واحترامهما في الإرسال**؛ فلا تعرض مفتاحاً يُخزَّن
  ولا يُطبَّق.
- **الإشعارات:** لا نقطة حذف/أرشفة، ولا تصفية على الخادم (page/limit فقط) —
  التصفية تجري على المحمّل، فأبقِ «تحميل المزيد» ونبّه أن الأقدم لم يُحمَّل.
  و`isLive` من `NotificationsContext` يكشف انقطاع البثّ اللحظي.
- **السحب بالفأرة على الويب يعمل عبر `PanResponder`** — تحقّقت منه بأحداث
  `mousedown/mousemove/mouseup` (أحداث `PointerEvent` وحدها لا تكفي).
- **رفع الصور غير مدعوم للحساب:** لا `expo-image-picker` في المشروع ولا نقطة
  رفع لصورة المستخدم (الموجودتان للمحادثات ووثائق المزوّد). لا تضع شارة
  كاميرا لا تفعل شيئاً.
- **جدار الاشتراك:** `services/paywall.js` يفرض حدّ تكرار (٢٤ ساعة لكل ميزة)،
  والسياق يأتي من `route.params.feature`. السعر يُجلب من الخطط الحقيقية —
  كان مكتوباً «$5 / شهرياً» بعملة وسعر لا وجود لهما.
- **بوابة الدفع المحاكاة محلياً معطّلة:** `generateCheckoutUrl` وwebhook
  المحاكاة يشيران إلى `localhost:3000/api/...` بينما الخادم على `3001`
  وبادئته `api/v1`. لإتمام شحن اختباري: أضف الرصيد في القاعدة يدوياً.
- **التخزين المؤقّت المكسور:** القراءة تُخزَّن تحت مفتاح فيه `page_/limit_`
  بينما الإبطال يحذف المفتاح المجرّد فقط، و`keys()` غير موجودة في
  cache-manager v7. الحلّ المعتمد في المشروع: **لا تخزين مؤقّت** لهذه القوائم
  الصغيرة (المركبات، سجل الصيانة، التذكيرات).

## الطبقة المشتركة — أعد الاستخدام قبل الإنشاء

- **الحالات الأربع:** `AsyncContent` في `components/ui.js` يقرّر بينها بقاعدة
  واحدة. معه `Skeleton` / `SkeletonCard` / `SkeletonList` / `ErrorState` /
  `StaleNotice`. **لا تبنِ حالات تحميل/فراغ/خطأ يدوياً.**
- **الحالات والدفع:** `services/orderStatus.js` هو المصدر الوحيد —
  `statusMeta` / `paymentMethodLabel` / `paymentStatusMeta` / `cancelReasonLabel`
  ودوال الصلاحية (`canCancel`…). والشارة الموحّدة `StatusPill` في `ui.js`
  (مع `TONE_PALETTE`) — لا تكرّر خريطة ألوان النغمات.
- **مواعيد الحجز:** `services/scheduling.js` (بناء الأيام والفتحات والتنسيق
  العربي بتوقيت دمشق). لا تُنسّق تاريخاً أو وقتاً عربياً يدوياً.
- **الاختيار بدل الكتابة:** `SelectField` + `PickerSheet` في `ui.js` (قائمة
  قابلة للبحث، عيّنات لونية، و«قيمة أخرى» كمخرج دائم). بيانات المركبات
  (شركات/طُرُز/ألوان/سنوات) في `screens/Vehicle/vehicleData.js`، والنموذج
  المشترك بين الإضافة والتحرير الموضعي هو `screens/Vehicle/VehicleForm.js`.
- **مسوّدات النماذج:** `services/draftStorage.js` — احفظ المسوّدة عند التغيير
  وامسحها بعد نجاح الحفظ.
- **السنوات:** `toLocaleString('ar-EG', { useGrouping: false })` — بدونها تظهر
  السنة «٢٬٠٢١».
- **أخطاء الحقول تُشتقّ من الحالة عبر `useEffect`، لا داخل المعالِجات:**
  الاختيار من قائمة ثم إغلاقها يقعان في نفس دورة الرسم، فحساب الخطأ داخل
  المعالج يقرأ قيمة قديمة ويعرض «أدخل الحقل» بعد ملئه مباشرةً.
- **الموقع:** `services/locationService.js` هو **المصدر الوحيد**؛ `expo-location`
  مستورد في هذا الملف فقط. `location.js` واجهة توافقية.
  القاعدة: **شاشة التمهيد وحدها تسأل النظام**؛ الجلب الخلفي يستهلك ولا يسأل.
  الإيماءة الصريحة (زر «موقعي الحالي») تمرّر `allowRequest: true`.
- **الحقول:** `InputField` و`PhoneField` يقبلان `ref` (تسلسل لوحة المفاتيح
  ونقل التركيز إلى أول حقل خاطئ)، ويضبطان `aria-invalid` و`aria-describedby`
  و`role="alert"` تلقائياً.
- **`useReducedMotion`** في `hooks/` — كل حركة دورية تُوقَف عنده.
- **`Alert.alert` ميت على الويب.** التأكيد بـ `ConfirmSheet`، والقوائم بـ `ActionSheet`.

## اختبار الحالات

أضف `?qa=<state>` إلى الرابط في وضع التطوير (`services/qa.js`) بدل تعديل
الكود مؤقّتاً. تُلغى تلقائياً خارج `__DEV__`.

`?qa=<step>` يقفز أيضاً إلى أي شاشة (مفاتيح الخطوات في `App.js`)، لكن الشاشة
تصل بلا `route.params`. لذلك `qaParams([...])` تقرأ المعطيات من الرابط:
`?qa=bookingNew&serviceId=…&providerId=…`.

## بيئة التشغيل

- خادم Expo يعمل على `localhost:8081`، والواجهة الخلفية على `localhost:3001`.
- `.claude/launch.json` يحوي مدخلاً **للارتباط** بالخادم القائم (`newapp2-web`)
  ومدخلاً للتشغيل البارد (`newapp2-web-cold`). التشغيل البارد يعمل من جذر
  المستودع فيلزمه `npm --prefix newapp2/carApp run web` — `npx expo start`
  المجرّدة تفشل بـ «package.json does not exist».
- ٤٠١ على `/auth/me` متبوعاً بـ `/auth/refresh-token` ثم ٢٠٠ هو **سلوك سليم**
  (تجديد التوكن)، لا عطل.
- **قبل تصديق أي سلوك خادم: تأكّد أن العملية التي تخدم ٣٠٠١ هي التي شغّلتها.**
  ضاع وقت في هذه الدفعة على مراقب `nest --watch` قديم من جلسة سابقة ظلّ حيّاً
  ويعيد البناء ويتسابق على المنفذ (`EADDRINUSE`)، فكانت النتائج من كود قديم.
  الفحص: `Get-CimInstance Win32_Process | ? { $_.CommandLine -like '*CAR_HERO_BACKEND*' }`.
- لوحة المتصفّح تعمل بـ `prefers-reduced-motion: reduce` مفعّلاً — أي أن مسار
  «تقليل الحركة» هو الافتراضي فيها.

## أصول تالفة

- `assets/slide5.jpg` مشوّهة (نصّها المطبوع «CARP HERQ») — لا تستخدمها.

## قرارات معلّقة تخصّ مالك المنتج

كلّها مرصودة أثناء العمل، ولا يمكن حسمها من طرف الواجهة لأنها قرارات عمل أو
تغييرات في الخادم:

1. **قيمة نقطة الوفاء ٠٫٠٥ ل.س** (`RedeemLoyaltyPointsUseCase.pointValue`) —
   ١٬٠٠٠ نقطة = ٥٠ ل.س، رقم لا يتناسب مع التسعير السوري.
2. **لا مسار يمنح النقاط** في الخادم إطلاقاً (تُخصم وتُعاد عند الإلغاء فقط)،
   فبرنامج الولاء معطّل عملياً.
3. **عملة المحفظة الافتراضية `SAR`** بينما كل المبالغ بالليرة — عولج عرضاً في
   العميل، والإصلاح الجذري في مخطّط المحفظة.
4. **فصل الإشعارات الحرجة عن التسويقية:** يحتاج حقلين في
   `preferences.notifications` + احترامهما في `notifications.service` قبل الإرسال.
5. **رفع صورة الملف الشخصي:** يحتاج `expo-image-picker` (غير مثبّت) ونقطة رفع
   لصورة المستخدم؛ `PATCH /users/me` يقبل `profileImage` كرابط أصلاً.
6. **تغيير رقم الهاتف:** لا حقل ولا نقطة نهاية — معروض معطّلاً مع مخرج للدعم.
7. **إضافة بطاقة مصرفية:** تتطلّب `providerToken` من بوّابة غير مدمجة.
8. **تخطّي موعد واحد في خطة الغسيل:** `UpdateWashPlanDto` لا يقبل
   `nextBookingAt` — المتاح إيقاف مؤقّت للخطة كلها.
9. **حذف/أرشفة إشعار:** لا نقطة نهاية (المتاح: قراءة فقط).
10. **بوّابة الدفع المحاكاة محلياً** تشير إلى `localhost:3000/api/...` بينما
    الخادم على `3001/api/v1` — تمنع اختبار الشحن من طرف إلى طرف.
